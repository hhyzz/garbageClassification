<van-tabbar active="{{ active }}" active-color="#222831" bind:change="onChange">
 <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>